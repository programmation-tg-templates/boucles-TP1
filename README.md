# Boucles - TP1 - Les étoiles

## Consignes

Ecrire une fonction qui permet de construire une chaîne de caractère constituée de `n` étoiles (caractère `*`).
Le nombre `n` d’étoiles est le paramètre de la fonction `boucle1`.
